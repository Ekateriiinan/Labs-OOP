#include <vector>

std::vector<int> toBinaryArray(unsigned int n) {
    std::vector<int> result(32, 0);
    
    for (int i = 0; i < 32; i++) {
        result[31 - i] = (n >> i) & 1;
    }
    
    return result;
}
